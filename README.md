# ACYE2_Conferencia2025

### Instalacion de SO

### Conexion SSH
```cmd
ssh <USUARIO_RASPBERRY>@<IP_DE_LA_RASPBERRY>
```

Posterior al comando, se debe de agregar la contraseña.

### Instalar Neofetch

```cmd
# Actualizacion
sudo apt update && sudo apt upgrade -y  
# Instalar Neofetch 
sudo apt install neofetch -y            
# Probar
neofetch                                
```